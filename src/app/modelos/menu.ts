export class Menu{
    id : number;
    nombre : string;
    ruta : string;

    constructor(id, nombre, ruta){
        this.id = id;
        this.nombre = nombre;
        this.ruta = ruta;

    }

}